// import app from '../../../../__mocks__/utils/server.mocks';

const ENDPOINT = '/script';

/**
 * @test { script.routes.js }
 */

// describe.only(`GET ${ENDPOINT}`, () => {
//   it('should return a random string', done => {
//     app.get(ENDPOINT).end((err, res) => {
//       const { body, status, } = res;
//       expect(status).toBe(200);
//       done();
//     });
//   });
// });
