// jest.mock('../../../__mocks__/request');

// import * as user from '@/components/users/user.controller';

// // describe('The script controller', () => {
// // it('should return the user', () => {
// //   let testons = script();
// //   expect(testons).toEqual('test');
// // });
// // });
// const userToSend = {
//   email: user.email,
//   username: user.username,
//   avatar: user.avatar,
// };

// it.only('works with promises', () => {
  
//   expect.assertions(1);
//   return user.getUser(1).then(data => expect(data).toEqual(userToSend));
// });
